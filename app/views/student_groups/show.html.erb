<p id="notice"><%= notice %></p>

<p>
  <strong><%=t('views.students_group.name')%>:</strong>
  <%= @student_group.name %>
</p>

<p>
  <strong><%=t('views.students_group.year')%>:</strong>
  <%= @student_group.year %>
</p>

<%= link_to t('edit'), edit_student_group_path(@student_group) %> |
<%= link_to t('back'), student_groups_path %>
