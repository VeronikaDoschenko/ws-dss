<%= form_for(@ws_model_status) do |f| %>
  <% if @ws_model_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ws_model_status.errors.count, "error") %> prohibited this ws_model_status from being saved:</h2>

      <ul>
      <% @ws_model_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
