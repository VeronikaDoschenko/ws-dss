<h3><%=t('views.admin.users.list_of_users') %></h3>
 
<table class="table">
  <tr>
    <th>email</th>
    <th>jbc</th>
    <th></th>
    <th></th>
    <th>reset_password_sent_at</th>
    <th>remember_created_at</th>
    <th>sign_in_count</th>
    <th>current_sign_in_at</th>
    <th>last_sign_in_at</th>
    <th>current_sign_in_ip</th>
    <th>last_sign_in_ip</th>
    <th>created_at</th>
    <th>updated_at</th>
    <th>confirmed_at</th>
    <th>confirmation_sent_at</th>
    <th>unconfirmed_email</th>
    <th>authentication_token</th>
  </tr>
 
<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= link_to user.ws_jobs.size.to_s, ws_jobs_url(q: {user_id_eq: user.id, s: "updated_at desc"}) %></td>
    <td><%= link_to t('edit'), edit_admin_user_path(user) %></td>
    <td><%= link_to t('remove'), admin_user_path(user), method: :delete, data: { confirm: t('are_you_sure') } %></td>
    <td><%= user.reset_password_sent_at %></td>
    <td><%= user.remember_created_at %></td>
    <td><%= user.sign_in_count %></td>
    <td><%= user.current_sign_in_at %></td>
    <td><%= user.last_sign_in_at %></td>
    <td><%= user.current_sign_in_ip %></td>
    <td><%= user.last_sign_in_ip %></td>
    <td><%= user.created_at %></td>
    <td><%= user.updated_at %></td>
    <td><%= user.confirmed_at %></td>
    <td><%= user.confirmation_sent_at %></td>
    <td><%= user.unconfirmed_email %></td>
    <td><%= user.authentication_token %></td>
  </tr>
<% end %>
</table>
